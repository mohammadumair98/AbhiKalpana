<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhikalpana">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ak"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".StudyLog"></activity>
        <activity android:name=".NestSelect" />
        <activity android:name=".AddMemberr" />
        <activity android:name=".MemberDetails" />
        <activity android:name=".CommonDashboard" />
        <activity android:name=".CaptainDashboard" />
        <activity android:name=".NestDataBase" />
        <activity android:name=".AttendancePage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>